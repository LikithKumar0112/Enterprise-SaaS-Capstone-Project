# Downloaded providers/modules - regenerated by `terraform init`, don't commit
.terraform/

# State files can contain secrets (passwords, keys, etc.) - never commit these
*.tfstate
*.tfstate.*

# Terraform crash logs
crash.log
crash.*.log

# Saved plan files
tfplan
